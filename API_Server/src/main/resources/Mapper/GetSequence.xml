<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.oc.api_server.Repository.GetSequence">

    <select id="nextReview1_id" resultType="Long">
        SELECT nextval(review1_id)
    </select>
</mapper>